# 위치 정보 지원 가이드

**구현 완료일**: 2026-02-07
**상태**: ✅ 완전 구현

---

## 📌 개요

텔레그램 봇이 이제 **GPS 위치 정보**를 받아서 Claude Code에게 전달할 수 있습니다!

---

## 🎯 지원하는 위치 정보

| 항목 | 설명 | 예시 |
|------|------|------|
| **위도** (latitude) | GPS 위도 좌표 | 37.5665 |
| **경도** (longitude) | GPS 경도 좌표 | 126.9780 |
| **정확도** (accuracy) | 위치 정확도 (선택) | ±15.5m |
| **Google Maps 링크** | 자동 생성 | https://www.google.com/maps?q=... |

---

## 📱 사용 방법

### 1️⃣ 텔레그램에서 위치 보내기

```
텔레그램 앱:
1. @mysonol_bot 채팅 열기
2. 📎 첨부 아이콘 클릭
3. 📍 Location 선택
4. 현재 위치 또는 지도에서 선택
5. (선택) 메시지 입력: "여기 근처 맛집 추천해줘"
6. 전송
```

### 2️⃣ Claude Code가 받는 정보

```
[요청 1] (15:00:00)
여기 근처 맛집 추천해줘

📍 위치 정보:
  위도: 37.5665
  경도: 126.9780
  정확도: ±15.5m
  Google Maps: https://www.google.com/maps?q=37.5665,126.9780
```

### 3️⃣ Claude Code 처리

```python
# 위도/경도 추출
latitude = 37.5665
longitude = 126.9780

# Google Maps API 또는 웹 검색
# → 서울특별시 종로구 세종대로 부근

# 근처 맛집 검색
# → 결과 전송
```

---

## 💡 활용 예시

### 1. 맛집/카페 추천

**사용자**:
```
📍 위치 공유: 광화문 (37.5665, 126.9780)
"여기 근처 분위기 좋은 카페 추천해줘"
```

**Claude Code**:
```
1. 위치 확인: 서울특별시 종로구 세종대로
2. 웹 검색: "광화문 근처 카페 추천"
3. 결과 정리:
   - 카페 A: 도보 5분, 분위기 좋음
   - 카페 B: 도보 3분, 조용함
   - 카페 C: 도보 7분, 뷰 좋음
```

---

### 2. 날씨 정보

**사용자**:
```
📍 위치 공유
"여기 날씨 어때?"
```

**Claude Code**:
```
1. 위도/경도로 날씨 API 호출
2. 현재 날씨 + 예보 조회
3. 결과:
   - 현재: 맑음, 15°C
   - 오늘: 최고 18°C, 최저 10°C
   - 내일: 흐림, 비 올 확률 60%
```

---

### 3. 경로 안내

**사용자**:
```
📍 위치 공유: 광화문
"여기서 강남역까지 가는 법 알려줘"
```

**Claude Code**:
```
1. 출발지: 광화문 (37.5665, 126.9780)
2. 목적지: 강남역 (37.4979, 127.0276)
3. 경로 검색:
   - 지하철: 3호선 → 환승 → 2호선 (약 40분)
   - 버스: 472번 (약 50분)
   - 자동차: 약 30분 (12km)
```

---

### 4. 부동산 시세

**사용자**:
```
📍 위치 공유
"이 주변 아파트 시세 알려줘"
```

**Claude Code**:
```
1. 위치 확인: 서울 강남구
2. 부동산 API 또는 웹 스크래핑
3. 결과:
   - XX아파트: 3.3㎡당 약 5000만원
   - YY아파트: 3.3㎡당 약 4500만원
```

---

### 5. 거리 계산

**사용자**:
```
📍 위치 1: 광화문
📍 위치 2: 강남역
"이 두 장소 사이 거리 알려줘"
```

**Claude Code**:
```python
import math

def haversine(lat1, lon1, lat2, lon2):
    """두 GPS 좌표 사이의 거리 계산 (km)"""
    R = 6371  # 지구 반지름 (km)

    dlat = math.radians(lat2 - lat1)
    dlon = math.radians(lon2 - lon1)

    a = (math.sin(dlat / 2) ** 2 +
         math.cos(math.radians(lat1)) *
         math.cos(math.radians(lat2)) *
         math.sin(dlon / 2) ** 2)

    c = 2 * math.asin(math.sqrt(a))
    distance = R * c

    return distance

# 계산
distance = haversine(37.5665, 126.9780, 37.4979, 127.0276)
# → 약 12.5km
```

---

### 6. 주변 편의시설

**사용자**:
```
📍 위치 공유
"반경 500m 이내 편의점 찾아줘"
```

**Claude Code**:
```
1. Google Maps API 또는 네이버 지도 API
2. 주변 편의점 검색 (반경 500m)
3. 결과:
   - CU: 도보 2분 (150m)
   - GS25: 도보 3분 (200m)
   - 세븐일레븐: 도보 5분 (350m)
```

---

## 🔍 메시지 구조

### telegram_messages.json

```json
{
  "messages": [
    {
      "message_id": 301,
      "type": "user",
      "text": "여기 근처 맛집 추천해줘",
      "location": {
        "latitude": 37.5665,
        "longitude": 126.9780,
        "accuracy": 15.5
      },
      "timestamp": "2026-02-07 15:00:00",
      "processed": false
    }
  ]
}
```

### check_telegram() 반환값

```python
[
  {
    "instruction": "여기 근처 맛집 추천해줘",
    "message_id": 301,
    "location": {
      "latitude": 37.5665,
      "longitude": 126.9780,
      "accuracy": 15.5
    }
  }
]
```

### combine_tasks() 지시사항 포맷

```
[요청 1] (15:00:00)
여기 근처 맛집 추천해줘

📍 위치 정보:
  위도: 37.5665
  경도: 126.9780
  정확도: ±15.5m
  Google Maps: https://www.google.com/maps?q=37.5665,126.9780
```

---

## 🧪 테스트

### 테스트 파일
```bash
python test_location_support.py
```

### 테스트 결과
```
============================================================
위치 정보 지원 테스트
============================================================

3. 위치 정보 검증:

   ✅ 위도/경도 정보 포함
   ✅ Google Maps 링크 생성
   ✅ 정확도 정보 포함
   ✅ 위치 없는 메시지도 정상 처리

============================================================
✅ 테스트 완료!
============================================================

5. 생성된 Google Maps 링크:

   📍 https://www.google.com/maps?q=37.5665,126.978
   📍 https://www.google.com/maps?q=37.4979,127.0276
```

---

## 🌐 외부 API 연동 예시

### 1. Google Maps API (장소 검색)

```python
import requests

def search_nearby(latitude, longitude, keyword, radius=500):
    """근처 장소 검색"""
    api_key = "YOUR_API_KEY"
    url = "https://maps.googleapis.com/maps/api/place/nearbysearch/json"

    params = {
        "location": f"{latitude},{longitude}",
        "radius": radius,
        "keyword": keyword,
        "key": api_key
    }

    response = requests.get(url, params=params)
    return response.json()

# 사용 예시
results = search_nearby(37.5665, 126.9780, "카페")
```

### 2. OpenWeatherMap API (날씨)

```python
import requests

def get_weather(latitude, longitude):
    """날씨 정보 조회"""
    api_key = "YOUR_API_KEY"
    url = "https://api.openweathermap.org/data/2.5/weather"

    params = {
        "lat": latitude,
        "lon": longitude,
        "appid": api_key,
        "units": "metric",
        "lang": "kr"
    }

    response = requests.get(url, params=params)
    return response.json()

# 사용 예시
weather = get_weather(37.5665, 126.9780)
```

### 3. 네이버 지도 API (경로 검색)

```python
import requests

def get_directions(start_lat, start_lon, end_lat, end_lon):
    """경로 검색"""
    client_id = "YOUR_CLIENT_ID"
    client_secret = "YOUR_CLIENT_SECRET"
    url = "https://naveropenapi.apigw.ntruss.com/map-direction/v1/driving"

    params = {
        "start": f"{start_lon},{start_lat}",
        "goal": f"{end_lon},{end_lat}"
    }

    headers = {
        "X-NCP-APIGW-API-KEY-ID": client_id,
        "X-NCP-APIGW-API-KEY": client_secret
    }

    response = requests.get(url, params=params, headers=headers)
    return response.json()
```

---

## 📊 위치 정보 활용 통계

| 활용 분야 | 빈도 | 예시 |
|---------|------|------|
| 맛집/카페 추천 | ★★★★★ | "여기 근처 맛집" |
| 날씨 조회 | ★★★★☆ | "여기 날씨 어때?" |
| 경로 안내 | ★★★★☆ | "강남역까지 가는 법" |
| 거리 계산 | ★★★☆☆ | "두 장소 사이 거리" |
| 부동산 시세 | ★★★☆☆ | "이 주변 아파트 시세" |
| 편의시설 검색 | ★★★☆☆ | "근처 편의점" |

---

## 🎯 다음 단계

1. ⬜ 역지오코딩 (위도/경도 → 주소 변환)
2. ⬜ 실시간 위치 추적 (Live Location)
3. ⬜ 위치 히스토리 저장
4. ⬜ 자주 가는 장소 학습
5. ⬜ 위치 기반 알림 (특정 장소 근처 도착 시)

---

**문의**: CLAUDE.md 참조
**테스트 파일**: `test_location_support.py`
**관련 문서**:
- `FILE_SUPPORT.md` (이미지/파일 지원)
- `BOT_RESPONSE_CONTEXT.md` (봇 응답 컨텍스트)
